# Moved

This document now lives in the **deeplakeclaude** project:

`~/Documents/code/deeplakeclaude/docs/RCLONE_TWICE_S3_SETUP.md`

The rclone remote template is there as `docs/rclone-twice-s3-remote.conf.example`; live keys stay in `docs/rclone-twice-s3-remote.conf` (gitignored in that repo).
